/**
 * (description)
 *
 * @author yourname
 */

export default class StepsController {
  constructor($state) {
    'ngInject';
    this.$state = $state;
    this.name = 'steps';

    this.stepsData = [
      {

      }
    ];
  }
}

/**
 * (description)
 * 
 * @author yourname
 */

export default class GeneratecomponentController {
  constructor($state) {
    'ngInject'
    this.$state = $state;
    this.name = 'generatecomponent';
  }
}
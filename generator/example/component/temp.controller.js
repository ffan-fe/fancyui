/**
 * (description)
 * 
 * @author yourname
 */

export default class <%= upCaseName %>Controller {
  constructor($state) {
    'ngInject'
    this.$state= $state;
    this.name = '<%= name %>';
  }
}